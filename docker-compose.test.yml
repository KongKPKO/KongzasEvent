services:
  app:
    container_name: eventqueue-test-app
    build:
      context: .
      dockerfile: Dockerfile.test
    ports:
      - "5173:5173"
    env_file:
      - .env.docker.test
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
